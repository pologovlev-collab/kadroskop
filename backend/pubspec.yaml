name: kadroskop_backend
description: API proxy and catalog normalizer for Kadroskop.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  crypto: ^3.0.7
  http: ^1.0.0
  shelf: ^1.0.0
  shelf_router: ^1.0.0
  sqlite3: ^3.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
